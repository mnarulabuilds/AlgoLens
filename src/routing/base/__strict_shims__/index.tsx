import type { ComponentType } from "react"

/** Stub used only by `tsconfig.strict.json` so Router can typecheck without the full dashboard tree. */
const Dashboard: ComponentType = () => null
export default Dashboard
